extern puts: function([]c_char) -> unit

effect Exception
    throw([]c_char) -> never
end

function main() -> i32 do
    try
        puts("say gex")
        throw("error occurred")
        puts("not printed")
    handle Exception throw(msg)
        puts("exception threw:")
        puts(msg)
        return 1
    end end
    return 0
end
